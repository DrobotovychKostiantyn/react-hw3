import React from 'react';
import s from './Title.module.css';

const SectionTitle = () => {
  return (
    <div className={s.blockTitle}>
      <h2 className={s.title}>Платформа для прохождения тестов</h2>
    </div>
  );
};

export default SectionTitle;
